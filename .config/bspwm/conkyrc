conky.config = {
    out_to_x = false,
    out_to_console = true,
    update_interval = 5,
}

conky.text =[[
C| \
MEM: $mem/$memmax | \
E: ${if_up enp0s31f6}${addr enp0s31f6}$endif | \
CPU: ${cpu cpu0}% | \
${wireless_essid} | \
${time} | \
]]
